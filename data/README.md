Put your SPY historical daily CSV here as `data/SPY.csv`.

Accepted format:
- Required columns: `Date` plus either `Close` or `Adj Close`
- Yahoo Finance export format works directly

Quick start with included sample:
- Copy `data/SPY.csv.example` to `data/SPY.csv`

